# https://docs.ansible.com/ansible/latest/collections/netbox/netbox/nb_inventory_inventory.html

plugin: netbox.netbox.nb_inventory
api_endpoint: https://netbox.labo.ovh

# Read only token, feel free to use it if my Netbox instance is up and running. No warranty provided :D
token: 60a3f8a9283a563860d253b49b63f4f6d741ff62
validate_certs: True

config_context: True

group_by:
  - site
  - role

# Filter out not connected items such as racks
device_query_filters:
  - has_primary_ip: 'true'

## Filter out decommissionned devices for example
# query_filters:
#  - status: active

keyed_groups:
  - prefix: status
    key: status.value